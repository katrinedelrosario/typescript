// percent calculating func
function calculatePercent(number: number, percent: number): number {

    return (
        number * percent
    )/ 100
}
const result = calculatePercent(200, 25) //startnumber, percent = result (in this case 50)

//


